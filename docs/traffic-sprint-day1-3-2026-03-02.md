# Day 1-3 SEO + CWV Execution (2026-03-02)

## Scope
- Day 1 baseline capture
- Day 2 technical SEO blockers
- Day 3 Core Web Vitals-focused code changes

## Baseline (Before)
- Build status: passes (`npm run build`)
- Main warnings:
  - `blog` chunk over 500 kB
  - `BlogPost` chunk over 500 kB
  - `vite-plugin-image-optimizer` missing optional deps: `sharp`, `svgo`
- Crawl/index checks:
  - `robots.txt` allows content sections and points to sitemap
  - `sitemap.xml` includes all current blog slugs (51/51 matched against source data)
  - Canonical tags generated by `SEOHead` based on current path

## Changes Applied
- Query result index-control:
  - `src/pages/Blog.tsx`
  - Added `noindex` when `?q=` is present to prevent low-value search-result URL indexation.

- Blog/article performance improvements:
  - `src/pages/BlogPost.tsx`
  - Converted heavy inline article tools/calculators to lazy imports with `Suspense` fallback.
  - Added hero image rendering hints: `loading="eager"`, `decoding="async"`, explicit `width`/`height`.

- Card image rendering hints:
  - `src/components/BlogCard.tsx`
  - Added `decoding="async"`, `fetchPriority="low"`, and explicit `width`/`height` to reduce layout instability risk.

- Sitemap freshness:
  - `public/sitemap.xml`
  - Updated `<lastmod>` values from `2026-02-24` to `2026-03-02`.

## Validation (After)
- Build status: passes (`npm run build`)
- Chunk result:
  - `BlogPost` reduced from ~`754.61 kB` to ~`22.35 kB` (route shell now loads widgets on demand)
  - `blog` remains ~`556.44 kB` (still above warning threshold)
- Remaining warnings:
  - `vite-plugin-image-optimizer` missing optional deps: `sharp`, `svgo`
  - one chunk still above 500 kB (`blog`)

## Constraints Kept
- Google Analytics was intentionally not modified.
- Video section remains removed from routing/navigation as requested previously.

## Remaining High-Impact Items (Next)
- Install optional optimization deps and re-test:
  - `npm i -D sharp svgo`
- Re-run build and compare chunk sizes after lazy-loading changes.
- Run Lighthouse on:
  - `/`
  - `/blog`
  - 2 highest-traffic `/blog/:slug` pages
- Continue with content sprint (6 intent-mapped pages + internal links).

## Day 4 Update (Chunk Split)
- Refactored blog data loading model:
  - Added lightweight metadata index: `src/data/articles/blog-meta.ts`
  - Added slug-based lazy loaders: `src/data/articles/blog-post-loaders.ts`
  - Split full article content into 9 lazy chunks under `src/data/articles/chunks/`
- Wiring changes:
  - `src/lib/blog.ts` now uses `blog-meta` (list pages no longer import full article HTML)
  - `src/pages/BlogPost.tsx` now lazy-loads only the requested article by slug
- Build result after split:
  - Previous blocking chunk: `blog ~556.44 kB`
  - New blog list chunk: `blog ~37.28 kB`
  - Full-content chunks split to ~`28.82 kB` to `79.74 kB` each
  - No `>500 kB` chunk-size warning remains
